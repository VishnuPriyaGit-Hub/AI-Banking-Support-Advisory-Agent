FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1
ENV PIP_NO_CACHE_DIR=1

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends build-essential curl \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --upgrade pip \
    && pip install -r requirements.txt

COPY app ./app
COPY prompts ./prompts
COPY data/phase5_supabase_schema.sql ./data/phase5_supabase_schema.sql

RUN mkdir -p /app/data /app/logs

EXPOSE 8501

CMD streamlit run app/ui/streamlit_app.py --server.address=0.0.0.0 --server.port=${PORT:-8501} --server.enableCORS=false --server.enableXsrfProtection=false
